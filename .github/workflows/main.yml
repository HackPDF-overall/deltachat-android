name: Android Build APK

on:
  push:
    branches: [ "master", "main" ]
  pull_request:
    branches: [ "master", "main" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'  # 必须改回 17
        distribution: 'temurin'
        cache: gradle

    - name: Grant execute permission for gradlew
      run: chmod +x gradlew

    - name: Build Debug APK
      # 关键修改：
      # 1. 使用 JDK 17 运行环境
      # 2. 加上 -x lint 跳过代码检查防止报错
      # 3. 加上 --stacktrace 以防万一出错能看到详细信息
      run: ./gradlew assembleDebug -x lint --stacktrace

       # 1. 侦察兵：直接用 Linux 命令全盘搜索，打印位置给你看
    - name: 🔍 Locate APK Files (Debug info)
      run: |
        echo "Searching for APK files..."
        find . -type f -name "*.apk"
        echo "Search finished."

    # 2. 收割机：根目录全图抓取
    - name: Upload ALL APKs
      uses: actions/upload-artifact@v4
      with:
        name: all-detected-apks
        path: "**/*.apk" 
        # 如果还是找不到，就警告，别报错中断
        if-no-files-found: warn
      with:
        name: app-debug
        path: app/build/outputs/apk/**/*.apk
